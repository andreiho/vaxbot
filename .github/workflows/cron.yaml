name: daily-run
on:
  schedule:
    - cron: "0 5 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request GET \
          --url 'https://vaxbot-andreiho.vercel.app/api/bot'
